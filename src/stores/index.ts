/**
 * stores 统一导出入口。
 *
 * 设计原因：
 * 1. 页面层后续统一从 `@/stores` 导入，避免每个文件都显式依赖具体 store 文件名。
 * 2. 当 stores 目录继续扩展时，只需要在这里补导出，不必全项目修改导入路径。
 * 3. 目录级出口也能让模块边界更清晰，便于后续按领域继续拆分状态管理。
 */
export { useAppStore } from './app'
